name: Deploy ARC

on:
  push:
    branches: [main]
    paths:
      - "arc-system/**"
      - "versions.env"
      - "install.sh"
  workflow_dispatch:   # allow manual re-deploy

permissions:
  contents: read
  id-token: write      # required for OIDC token exchange with AWS

jobs:
  deploy:
    name: Helm deploy ARC
    runs-on: ubuntu-latest   # bootstrap uses GitHub-hosted until ARC is up

    steps:
      - uses: actions/checkout@v4

      - name: Load pinned versions
        run: |
          set -a && source versions.env && set +a
          echo "ARC_CHART_VERSION=${ARC_CHART_VERSION}" >> "$GITHUB_ENV"

      - name: Configure kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: "v1.29.0"

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: "v3.14.0"

      - name: Authenticate to AWS via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/${{ secrets.OIDC_ROLE_NAME }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Configure kubectl for EKS
        run: |
          aws eks update-kubeconfig \
            --region ${{ secrets.AWS_REGION }} \
            --name ${{ secrets.EKS_CLUSTER_NAME }}

      - name: Apply RBAC
        run: kubectl apply -f arc-system/rbac.yaml

      - name: Apply NetworkPolicies
        run: kubectl apply -f arc-system/network-policy.yaml

      - name: Deploy ARC controller
        run: |
          helm upgrade --install arc \
            oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set-controller \
            --version "$ARC_CHART_VERSION" \
            --namespace arc-system \
            --create-namespace \
            --values arc-system/arc-controller-values.yaml \
            --wait --timeout 5m

      - name: Deploy runner scale set
        run: |
          helm upgrade --install arc-runner-set \
            oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set \
            --version "$ARC_CHART_VERSION" \
            --namespace arc-runners \
            --create-namespace \
            --values arc-system/arc-runner-scale-set-values.yaml \
            --wait --timeout 5m

      - name: Apply ServiceMonitors
        run: kubectl apply -f arc-system/service-monitor.yaml

      - name: Verify rollout
        run: |
          kubectl rollout status deployment/arc-gha-runner-scale-set-controller \
            -n arc-system --timeout=120s
          kubectl get autoscalingrunnerset -n arc-runners
